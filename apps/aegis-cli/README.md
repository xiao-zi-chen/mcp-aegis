# aegis-cli

Planned responsibility:

- resolve install requests against MCP Aegis policy
- fetch approved metadata from the subregistry
- generate host-specific MCP configuration

Suggested stack:

- Go

Initial scope:

- validate policy bundle
- resolve package version
- print install decision trace
