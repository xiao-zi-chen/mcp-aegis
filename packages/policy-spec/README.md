# policy-spec

This package contains the policy contract for MCP Aegis.

Contents:

- `schema.json`
- `examples/default-policy.yaml`

The policy contract must remain deterministic, explainable, and strict enough for audit replay.
